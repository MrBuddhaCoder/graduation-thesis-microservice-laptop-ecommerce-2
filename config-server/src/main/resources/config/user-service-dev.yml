spring:
  datasource:
    url: jdbc:mysql://localhost:3306/laptop_ecommerce_graduation_project_user_service?serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: root
  rabbitmq:
    host: localhost

frontend:
  url: http://localhost:5173

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
